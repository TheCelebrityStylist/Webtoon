# Security and privacy

Projects are private by default. Auth and workspace membership gate project records, saves, AI, search, and exports. Security headers deny framing and browser capabilities. OAuth uses user-bound expiring state; integration refresh tokens are authenticated-encrypted; disconnect revokes before clearing. Manuscript text and tokens must never enter analytics or logs. Rate limiting, audit logs, deletion workflows, and signed object storage remain release gates.
