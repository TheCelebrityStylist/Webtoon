import{createCipheriv,createDecipheriv,createHash,createHmac,randomBytes,timingSafeEqual}from"node:crypto";
const key=()=>{const raw=process.env.INTEGRATION_ENCRYPTION_KEY;if(!raw)throw new Error("INTEGRATION_ENCRYPTION_KEY is required");const value=Buffer.from(raw,"base64");if(value.length!==32)throw new Error("INTEGRATION_ENCRYPTION_KEY must be 32 bytes in base64");return value};
export function encryptToken(token:string){const iv=randomBytes(12);const cipher=createCipheriv("aes-256-gcm",key(),iv);const encrypted=Buffer.concat([cipher.update(token,"utf8"),cipher.final()]);return ["v1",iv.toString("base64url"),cipher.getAuthTag().toString("base64url"),encrypted.toString("base64url")].join(".")}
export function decryptToken(value:string){const[v,iv,tag,data]=value.split(".");if(v!=="v1"||!iv||!tag||!data)throw new Error("Invalid encrypted token");const decipher=createDecipheriv("aes-256-gcm",key(),Buffer.from(iv,"base64url"));decipher.setAuthTag(Buffer.from(tag,"base64url"));return Buffer.concat([decipher.update(Buffer.from(data,"base64url")),decipher.final()]).toString("utf8")}
export function signOAuthState(userId:string,nonce:string,expiresAt:number){const payload=Buffer.from(JSON.stringify({userId,nonce,expiresAt})).toString("base64url");const secret=process.env.AUTH_SECRET;if(!secret)throw new Error("AUTH_SECRET is required");const signature=createHmac("sha256",secret).update(payload).digest("base64url");return `${payload}.${signature}`}
export function verifyOAuthState(state:string,userId:string,now=Date.now()){const[payload,signature]=state.split(".");if(!payload||!signature)return false;const secret=process.env.AUTH_SECRET;if(!secret)return false;const expected=createHmac("sha256",secret).update(payload).digest();const actual=Buffer.from(signature,"base64url");if(actual.length!==expected.length||!timingSafeEqual(actual,expected))return false;const parsed=JSON.parse(Buffer.from(payload,"base64url").toString())as{userId:string;expiresAt:number};return parsed.userId===userId&&parsed.expiresAt>now}
export function oauthStatePayload(state:string,userId:string,now=Date.now()){if(!verifyOAuthState(state,userId,now))return null;try{return JSON.parse(Buffer.from(state.split(".")[0],"base64url").toString())as{userId:string;nonce:string;expiresAt:number}}catch{return null}}
export function oauthNonceHash(nonce:string){return createHash("sha256").update(nonce).digest("hex")}
