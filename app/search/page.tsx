export default async function Search({ searchParams }: { searchParams: Promise<{ q?: string }> }) { const { q } = await searchParams; return <main className="public-page"><p className="eyebrow">SEARCH</p><h1>{q ? `Results for “${q}”` : "Find your next world."}</h1><form className="search"><input name="q" defaultValue={q} placeholder="Title, creator, or genre"/><button>Search</button></form><p className="empty">No published series match this search.</p></main>; }
