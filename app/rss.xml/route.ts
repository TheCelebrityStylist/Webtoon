import{articles}from"@/lib/blog";import{site}from"@/lib/site";const esc=(s:string)=>s.replace(/[<>&'\"]/g,c=>({"<":"&lt;",">":"&gt;","&":"&amp;","'":"&apos;",'"':"&quot;"}[c]??c));export function GET(){const items=articles.map(a=>`<item><title>${esc(a.title)}</title><link>${site.url}/blog/${a.slug}</link><guid>${site.url}/blog/${a.slug}</guid><description>${esc(a.excerpt)}</description><pubDate>${new Date(a.publishedAt).toUTCString()}</pubDate></item>`).join("");return new Response(`<?xml version="1.0"?><rss version="2.0"><channel><title>${site.name} Editorial</title><link>${site.url}/blog</link><description>${esc(site.description)}</description>${items}</channel></rss>`,{headers:{"content-type":"application/rss+xml; charset=utf-8"}})}
