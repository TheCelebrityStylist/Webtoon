import{auth}from"@/auth";import{prisma}from"@/lib/prisma";import{encryptToken,verifyOAuthState}from"@/integrations/google/security";
type TokenResponse={refresh_token?:string;scope?:string;error?:string};export async function GET(request:Request){const session=await auth();if(!session?.user?.id)return new Response("Unauthorized",{status:401});const url=new URL(request.url);const code=url.searchParams.get("code");const state=url.searchParams.get("state");if(!code||!state||!verifyOAuthState(state,session.user.id))return new Response("Invalid or expired OAuth state",{status:400});const clientId=process.env.GOOGLE_CLIENT_ID,clientSecret=process.env.GOOGLE_CLIENT_SECRET,redirectUri=process.env.GOOGLE_INTEGRATION_REDIRECT_URI;if(!clientId||!clientSecret||!redirectUri)return new Response("Google integration is not configured",{status:503});const response=await fetch("https://oauth2.googleapis.com/token",{method:"POST",headers:{"content-type":"application/x-www-form-urlencoded"},body:new URLSearchParams({code,client_id:clientId,client_secret:clientSecret,redirect_uri:redirectUri,grant_type:"authorization_code"}),cache:"no-store"});const tokens=await response.json()as TokenResponse;if(!response.ok||!tokens.refresh_token)return new Response("Google did not return a reusable permission grant",{status:400});await prisma.integrationConnection.upsert({where:{userId_provider:{userId:session.user.id,provider:"google"}},create:{userId:session.user.id,provider:"google",encryptedRefreshToken:encryptToken(tokens.refresh_token),grantedScopes:tokens.scope?.split(" ").filter(Boolean)??[]},update:{encryptedRefreshToken:encryptToken(tokens.refresh_token),grantedScopes:tokens.scope?.split(" ").filter(Boolean)??[],revokedAt:null}});return Response.redirect(new URL("/studio/settings/integrations/google",url))}
