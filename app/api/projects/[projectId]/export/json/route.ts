import{auth}from"@/auth";import{prisma}from"@/lib/prisma";import{requireProjectAccess}from"@/server/authorization";
export async function GET(_request:Request,{params}:{params:Promise<{projectId:string}>}){const session=await auth();if(!session?.user?.id)return new Response("Unauthorized",{status:401});const{projectId}=await params;try{await requireProjectAccess(session.user.id,projectId);const project=await prisma.series.findFirst({where:{id:projectId,deletedAt:null},include:{chapters:{where:{deletedAt:null},orderBy:{number:"asc"},include:{scenes:{where:{deletedAt:null},orderBy:{position:"asc"},include:{beats:{orderBy:{position:"asc"}}}}}},characters:{where:{deletedAt:null}},locations:{where:{deletedAt:null}},worldRules:{where:{deletedAt:null}}}});if(!project)return new Response("Not found",{status:404});const archive={format:"morrow-project-archive",schemaVersion:1,createdAt:new Date().toISOString(),project};return new Response(JSON.stringify(archive,null,2),{headers:{"content-type":"application/json; charset=utf-8","content-disposition":`attachment; filename="${project.slug}-morrow-backup.json"`,"cache-control":"private, no-store"}})}catch{return new Response("Forbidden",{status:403})}}
