import { auth } from "@/auth";import { requireProjectAccess } from "@/server/authorization";import { prisma } from "@/lib/prisma";
export async function GET(_:Request,{params}:{params:Promise<{projectId:string}>}){const session=await auth();if(!session?.user?.id)return new Response("Unauthorized",{status:401});const{projectId}=await params;try{await requireProjectAccess(session.user.id,projectId);const project=await prisma.series.findUniqueOrThrow({where:{id:projectId},include:{chapters:{where:{deletedAt:null},orderBy:{number:"asc"},include:{scenes:{where:{deletedAt:null},orderBy:{position:"asc"}}}}}});const markdown=[`# ${project.title}`,`> ${project.premise}`,...project.chapters.flatMap(c=>[`\n## ${c.number}. ${c.title}`,...c.scenes.flatMap(s=>[`\n### ${s.title}`,s.manuscriptText])])].join("\n\n");return new Response(markdown,{headers:{"content-type":"text/markdown; charset=utf-8","content-disposition":`attachment; filename="${project.slug}.md"`,"cache-control":"private, no-store"}})}catch{return new Response("Forbidden",{status:403})}}
