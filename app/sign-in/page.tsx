import Link from "next/link"; import { ActionForm, Field } from "@/components/ActionForm"; import { signInAction,signInGoogle } from "@/app/actions/auth";
export default function SignInPage() { return <main className="auth-page"><section><p className="eyebrow">PANELFORGE STUDIO</p><h1>Welcome back.</h1><p>Return to your private projects and manuscripts.</p></section><div><ActionForm action={signInAction} submitLabel="Sign in"><Field name="email" label="Email" type="email" required/><Field name="password" label="Password" type="password" required/><p className="muted">New here? <Link href="/sign-up">Create an account</Link></p></ActionForm>{process.env.GOOGLE_CLIENT_ID&&<form action={signInGoogle}><button className="button" type="submit">Continue with Google</button></form>}</div></main>; }
