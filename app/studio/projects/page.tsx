import Link from "next/link"; import { prisma } from "@/lib/prisma"; import { requireUser } from "@/server/session";
export default async function ProjectsPage() { const user = await requireUser(); const projects = await prisma.series.findMany({ where: { deletedAt: null, workspace: { members: { some: { userId: user.id } } } }, orderBy: { updatedAt: "desc" } }); return <main className="studio-content"><div className="page-heading"><div><p className="eyebrow">YOUR LIBRARY</p><h1>Projects</h1></div><Link className="button" href="/studio/projects/new">New project</Link></div>{projects.length ? <div className="project-list">{projects.map(p=><Link key={p.id} href={`/studio/projects/${p.id}/overview`}><span><strong>{p.title}</strong><small>{p.logline}</small></span><em>{p.status}</em></Link>)}</div> : <section className="empty"><h2>Your first world starts here.</h2><p>Create a project to connect its story bible, characters, and production work.</p><Link className="button" href="/studio/projects/new">Create a project</Link></section>}</main>; }
