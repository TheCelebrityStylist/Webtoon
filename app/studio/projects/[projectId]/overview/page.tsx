import Link from "next/link"; import { notFound } from "next/navigation"; import { prisma } from "@/lib/prisma";
export default async function Overview({ params }: { params: Promise<{ projectId: string }> }) { const { projectId } = await params; const p = await prisma.series.findFirst({ where: { id: projectId, deletedAt: null }, include: { _count: { select: { characters: true, locations: true, worldRules: true } } } }); if (!p) notFound(); return <main className="studio-content"><div className="page-heading"><div><p className="eyebrow">{p.genre.replaceAll("_", " ")} · {p.status}</p><h1>{p.title}</h1><p className="lead">{p.logline}</p></div><Link className="button secondary" href={`/studio/projects/${p.id}/settings`}>Edit project</Link></div><section className="overview-grid"><article><span>Characters</span><strong>{p._count.characters}</strong></article><article><span>Locations</span><strong>{p._count.locations}</strong></article><article><span>World rules</span><strong>{p._count.worldRules}</strong></article></section><section className="editorial-block"><p className="eyebrow">SYNOPSIS</p><p>{p.synopsis}</p><p className="muted">For {p.audience} · {p.language}</p></section></main>; }
