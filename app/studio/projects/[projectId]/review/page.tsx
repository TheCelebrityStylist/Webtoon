import{prisma}from"@/lib/prisma";
export default async function Review({params}:{params:Promise<{projectId:string}>}){const{projectId}=await params;const findings=await prisma.revisionFinding.findMany({where:{seriesId:projectId,status:"OPEN"},orderBy:{createdAt:"desc"}});return <main className="studio-content"><p className="eyebrow">FOCUSED REVISION</p><h1>Review</h1>{findings.length?<div className="review-queue">{findings.map(x=><article key={x.id}><small>{x.category} · {x.importance}</small><blockquote>{x.passage}</blockquote><p>Open the source passage to inspect the evidence and choose what becomes canon.</p></article>)}</div>:<div className="empty"><h2>No decisions need you right now.</h2><p>Keep writing. Morrow will bring forward evidence-backed questions without interrupting the page.</p></div>}</main>}
