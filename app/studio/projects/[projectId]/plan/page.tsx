import Link from"next/link";import{prisma}from"@/lib/prisma";
export default async function Plan({params}:{params:Promise<{projectId:string}>}){const{projectId}=await params;const chapters=await prisma.chapter.findMany({where:{seriesId:projectId,deletedAt:null},orderBy:{number:"asc"},include:{scenes:{where:{deletedAt:null},orderBy:{position:"asc"}}}});return <main className="studio-content"><header className="page-heading"><div><p className="eyebrow">SHAPE THE STORY</p><h1>Plan</h1></div><nav className="view-switch"><b>Outline</b><span>Board</span><span>Cards</span><span>Plot threads</span></nav></header><section className="plan-outline">{chapters.map(ch=><article key={ch.id}><header><small>{ch.kind} {ch.number}</small><h2>{ch.title}</h2></header>{ch.scenes.map(scene=><Link key={scene.id} href={`/studio/projects/${projectId}/chapters/${scene.id}`}><span>⠿</span><b>{scene.title}</b><small>{scene.manuscriptText.trim().split(/\s+/).filter(Boolean).length} words</small></Link>)}</article>)}{!chapters.length&&<div className="empty"><h2>Your outline is open.</h2><p>Press Create to add a chapter or scene. You only need a title.</p></div>}</section></main>}
