import { notFound } from "next/navigation"; import { prisma } from "@/lib/prisma"; import { ProjectForm } from "@/components/ProjectForm"; import { deleteProject, updateProject } from "@/app/actions/projects"; import { DeleteProjectForm } from "@/components/DeleteProjectForm";
export default async function Settings({ params }: { params: Promise<{ projectId: string }> }) { const { projectId } = await params; const project = await prisma.series.findFirst({ where: { id: projectId, deletedAt: null } }); if (!project) notFound(); const update = updateProject.bind(null, projectId); const remove = deleteProject.bind(null, projectId); return <main className="studio-content narrow"><p className="eyebrow">PROJECT SETTINGS</p><h1>Edit the foundation.</h1><ProjectForm action={update} project={project}/><section className="danger-zone"><h2>Delete project</h2><p>This hides the project and preserves its audit history. Confirm the project title in the browser prompt to continue.</p><DeleteProjectForm action={remove} title={project.title}/></section></main>; }
