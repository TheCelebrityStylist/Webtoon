import Link from "next/link"; import { requireUser } from "@/server/session"; import { signOutAction } from "@/app/actions/auth";
export default async function StudioLayout({ children }: { children: React.ReactNode }) { const user = await requireUser(); return <div className="studio-shell"><aside><Link className="studio-brand" href="/studio">PF</Link><nav><Link href="/studio/projects">Projects</Link><span>Recent</span><span>Notifications</span></nav><form action={signOutAction}><button className="quiet-button">Sign out</button></form></aside><div className="studio-main"><header><span>Creator studio</span><span>{user.email}</span></header>{children}</div></div>; }
