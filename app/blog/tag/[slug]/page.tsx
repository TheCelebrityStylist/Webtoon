import{notFound}from"next/navigation";import{MarketingShell}from"@/components/MarketingShell";import{ArticleCard}from"@/components/ArticleCard";import{articles}from"@/lib/blog";export default async function Tag({params}:{params:Promise<{slug:string}>}){const slug=(await params).slug;const list=articles.filter(a=>a.tags.includes(slug));if(!list.length)notFound();return <MarketingShell><main className="blog-index"><header><p className="eyebrow">TAG</p><h1>#{slug}</h1></header><div className="article-grid">{list.map(a=><ArticleCard key={a.slug} article={a}/>)}</div></main></MarketingShell>}
