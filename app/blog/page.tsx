import type{Metadata}from"next";import{MarketingShell}from"@/components/MarketingShell";import{ArticleCard}from"@/components/ArticleCard";import{articles}from"@/lib/blog";import{brand}from"@/lib/site";export const metadata:Metadata={title:"Writing craft and story systems",description:"Practical editorial guides for planning, continuity, revision, translation, and long-form writing."};export default function Blog(){const[featured,...rest]=articles;return <MarketingShell><main className="blog-index"><header><p className="eyebrow">{brand.marks.wordmark} EDITORIAL</p><h1>Better decisions make better stories.</h1><p>Product-owned guides about writing craft, continuity, revision, and multilingual story development. No fabricated research or borrowed authority.</p></header>{featured&&<section className="proof-console"><div><small>FEATURED EDITORIAL</small><h2>{featured.title}</h2><p>{featured.excerpt}</p></div><output><span>INTERACTIVE METHOD</span><strong>Before state → scene pressure → consequential change → after state</strong><a className="button signal" href={`/blog/${featured.slug}`}>Read and apply →</a></output></section>}<nav>{[...new Set(articles.map(a=>a.category))].map(c=><a key={c} href={`/blog/category/${c}`}>{c.replaceAll("-"," ")}</a>)}</nav><div className="article-grid">{rest.map(a=><ArticleCard article={a} key={a.slug}/>)}</div></main></MarketingShell>}
