import type{Metadata}from"next";import{MarketingShell}from"@/components/MarketingShell";import{ArticleCard}from"@/components/ArticleCard";import{articles}from"@/lib/blog";export const metadata:Metadata={title:"Writing craft and story systems",description:"Practical editorial guides for planning, continuity, revision, translation, and long-form writing."};export default function Blog(){return <MarketingShell><main className="blog-index"><header><p className="eyebrow">PANELFORGE EDITORIAL</p><h1>Better decisions make better stories.</h1><p>Product-owned guides about writing craft, continuity, revision, and multilingual story development. No fabricated research or borrowed authority.</p></header><nav>{[...new Set(articles.map(a=>a.category))].map(c=><a key={c} href={`/blog/category/${c}`}>{c.replaceAll("-"," ")}</a>)}</nav><div className="article-grid">{articles.map(a=><ArticleCard article={a} key={a.slug}/>)}</div></main></MarketingShell>}
