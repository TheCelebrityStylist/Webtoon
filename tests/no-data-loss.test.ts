import{describe,expect,it}from"vitest";import{chooseRecoveryDraft,createDraft,retryDelay}from"@/lib/persistence/manuscript";
describe("no data loss policies",()=>{it("recovers a locally journaled edit at the same server revision",()=>{const draft=createDraft("project","scene",4,{type:"doc"},"Seven hundred protected words");expect(chooseRecoveryDraft(4,draft)?.text).toContain("protected")});it("does not silently apply a draft based on an impossible future server revision",()=>{const draft=createDraft("project","scene",8,{type:"doc"},"conflicting text");expect(chooseRecoveryDraft(7,draft)).toBeNull()});it("uses bounded exponential retry delays",()=>{expect(retryDelay(0)).toBe(750);expect(retryDelay(3)).toBe(6000);expect(retryDelay(20)).toBe(30000)})});
