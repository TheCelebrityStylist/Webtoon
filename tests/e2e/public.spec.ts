import { test,expect } from "@playwright/test";
test("private writing entry points resolve without reader routes",async({request})=>{for(const path of ["/","/about","/sign-in","/sign-up"]){const response=await request.get(path);expect(response.ok()).toBe(true)}expect((await request.get("/discover")).status()).toBe(404)});
test("commercial and editorial routes resolve with meaningful content",async({request})=>{for(const path of ["/product","/writing","/planning","/continuity","/revision","/languages","/google","/pricing","/security","/blog","/blog/how-to-plan-a-novel","/rss.xml"]){const response=await request.get(path);expect(response.ok(),path).toBe(true);expect((await response.text()).length).toBeGreaterThan(300)}});
test("format, intelligence, Google, privacy, and regional-language proof routes resolve",async({request})=>{for(const path of ["/story-intelligence","/story-graph","/characters","/relationships","/timeline","/formats","/novel-writing","/screenwriting","/webtoon-writing","/game-writing","/google-docs","/google-sheets","/google-drive","/google-calendar","/privacy","/languages/english","/languages/dutch","/languages/german","/languages/spanish","/languages/portuguese"]){const response=await request.get(path);expect(response.ok(),path).toBe(true);const html=await response.text();expect(html).toContain('aria-label="Main navigation"');expect(html.length).toBeGreaterThan(1000)}});
test("landing contains accessible writing interactions and conversion landmarks",async({request})=>{const html=await(await request.get("/")).text();expect(html).toContain('aria-label="Main navigation"');expect(html).toContain("Every writer knows the feeling");expect(html).toContain("Start writing free");expect(html).toContain("Your whole story")});
test("homepage continuity moment exposes evidence and applies a decision",async({page})=>{await page.goto("/");await page.getByRole("button",{name:"the silver key"}).click();await expect(page.getByText("Chapter 5 says Lena kept the key after Tomas left.")).toBeVisible();await page.getByRole("button",{name:"Revise sentence"}).click();await expect(page.getByText("meant to hand him",{exact:false})).toBeVisible()});
test("formats transform navigator, editor language, and metadata",async({page})=>{await page.goto("/formats");await page.getByRole("button",{name:"Screenplay",exact:true}).click();await expect(page.getByText("INT. MUSEUM — NIGHT",{exact:true})).toBeVisible();await expect(page.getByText("Page 47 of 108",{exact:true})).toBeVisible();await page.getByRole("button",{name:"Game narrative",exact:true}).click();await expect(page.getByText("Requires trust ≥ 4",{exact:true})).toBeVisible();await expect(page.getByRole("button",{name:"Give him the key"})).toBeVisible()});
test("pricing chooser changes its recommendation without hiding limits",async({page})=>{await page.goto("/pricing");await expect(page.getByText("Writer",{exact:true}).first()).toBeVisible();await page.getByLabel("I translate my work").check();await expect(page.getByText("Professional",{exact:true}).first()).toBeVisible();await page.getByText("Compare limits and capabilities").click();await expect(page.getByRole("cell",{name:"10"})).toBeVisible()});
