import{describe,expect,it}from"vitest";import{findPerspectiveConflicts,previewConsequences,reconstructState,reconstructWorld,type Transition}from"@/lib/canon/engine";
const transitions:Transition[]=[{id:"key-library",entityId:"key",sequence:2,property:"owner",afterValue:"Lena",status:"CONFIRMED"},{id:"key-tomas",entityId:"key",sequence:7,property:"owner",beforeValue:"Lena",afterValue:"Tomas",status:"CONFIRMED",dependsOn:["key-library"]},{id:"coat-pocket",entityId:"key",sequence:7,property:"location",afterValue:"Coat pocket",status:"CONFIRMED",dependsOn:["key-tomas"]},{id:"future-reference",entityId:"scene-18",sequence:18,property:"requiresKeyOwner",afterValue:"Tomas",status:"CONFIRMED",dependsOn:["key-tomas"]}];
describe("Canon Engine",()=>{it("reconstructs an entity at any story moment",()=>{expect(reconstructState("key",2,transitions).values.owner).toBe("Lena");expect(reconstructState("key",7,transitions).values).toEqual({owner:"Tomas",location:"Coat pocket"})});it("reconstructs the whole world deterministically",()=>{expect(reconstructWorld(18,transitions).key.values.owner).toBe("Tomas")});it("previews transitive consequences before mutation",()=>{expect(previewConsequences(["key-library"],transitions).map(x=>x.transitionId)).toEqual(["key-tomas","coat-pocket","future-reference"])});it("separates truth from belief and intentional deception",()=>{const conflicts=findPerspectiveConflicts([{subject:"murder",predicate:"killer",perspective:"REALITY",value:"Lena",sequence:4},{subject:"murder",predicate:"killer",perspective:"READER",value:"Tomas",sequence:5},{subject:"murder",predicate:"killer",perspective:"CHARACTER_BELIEF",perspectiveEntityId:"Tomas",value:"accident",sequence:5}]);expect(conflicts).toHaveLength(2);expect(conflicts[1].intentional).toBe(true)})});
