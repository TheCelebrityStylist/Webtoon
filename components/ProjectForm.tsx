import { ActionForm, Field } from "./ActionForm"; import type { FormState } from "@/app/actions/auth";
type Project = { title: string; logline: string; synopsis: string; genre: string; language: string; audience: string };
export function ProjectForm({ action, project }: { action: (state: FormState, data: FormData) => Promise<FormState>; project?: Project }) { return <ActionForm action={action} submitLabel={project ? "Save changes" : "Create draft"}><Field name="title" label="Title" defaultValue={project?.title} required/><Field name="logline" label="Logline" defaultValue={project?.logline} textarea required/><Field name="synopsis" label="Synopsis" defaultValue={project?.synopsis} textarea required/><label><span>Genre</span><select name="genre" defaultValue={project?.genre ?? "DRAMA"}>{["ACTION","COMEDY","DRAMA","FANTASY","HORROR","MYSTERY","ROMANCE","SCI_FI","SLICE_OF_LIFE"].map(x=><option key={x}>{x}</option>)}</select></label><Field name="language" label="Language code" defaultValue={project?.language ?? "en"} required/><Field name="audience" label="Intended audience" defaultValue={project?.audience} required/></ActionForm>; }
