import { setInterfaceLocale } from "@/app/actions/locale"; import type { InterfaceLocale } from "@/lib/i18n";
export function LocaleSwitcher({locale,path,label}:{locale:InterfaceLocale;path:string;label:string}) { return <form action={setInterfaceLocale} className="locale-switcher"><input type="hidden" name="returnTo" value={path}/><label><span>{label}</span><select name="locale" defaultValue={locale} onChange={(e)=>e.currentTarget.form?.requestSubmit()}>{[["en","English"],["nl","Nederlands"],["de","Deutsch"],["es","Español"],["pt","Português"]].map(([v,n])=><option key={v} value={v}>{n}</option>)}</select></label></form>; }
