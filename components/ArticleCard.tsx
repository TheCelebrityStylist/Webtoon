import Link from"next/link";import{readingMinutes,type BlogArticle}from"@/lib/blog";export function ArticleCard({article}:{article:BlogArticle}){return <article className="article-card"><small>{article.category.replaceAll("-"," ")} · {readingMinutes(article)} min</small><h2><Link href={`/blog/${article.slug}`}>{article.title}</Link></h2><p>{article.excerpt}</p><div>{article.tags.map(t=><Link key={t} href={`/blog/tag/${t}`}>#{t}</Link>)}</div></article>}
